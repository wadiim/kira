#!/usr/bin/env ruby
# -*- coding: utf-8 -*-

require 'kira'

begin
  input = $stdin.read
  sudoku = Kira::Sudoku.new(input)
  sudoku.solve
  puts sudoku.to_s + "\n"
rescue StandardError => e
  puts "Error: " + e.message
end
